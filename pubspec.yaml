name: din
description: Din is a web framework written in Dart, inspired by Golang Gin.
version: 0.1.0
repository: https://github.com/ggdream/din

environment:
  sdk: ^3.5.4

# Add regular dependencies here.
dependencies:

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
